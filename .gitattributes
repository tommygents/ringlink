# Normalize line endings: store as LF in the repo, check out native.
# Prevents CRLF/LF churn when collaborating across Windows and macOS/Linux.
* text=auto eol=lf
*.py text eol=lf

# Recorded hardware traces are data fixtures — keep them byte-stable.
traces/** -text
